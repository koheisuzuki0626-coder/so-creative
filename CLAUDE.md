# このリポジトリで作業するときの決まり

## バックグラウンドで走らせたものは、必ず自分で止める

**返事をする前に、走らせたものが全部止まっているか毎回確かめること。**
言われてから探すのでは遅い。

確かめ方（このコマンドで、セッション自身以外が出なければよい）:

```
ps -eo pid,etime,args --sort=start_time | grep -v '\[' \
  | grep -v 'process_api\|telemetry\|environment-manager\|/opt/claude-code/bin/claude'
```

止め方は `TaskStop`。プロセスを直接 `pkill -f <文字列>` で狙うときは、
**自分の実行中のシェルのコマンド文字列にもその文字列が入っている**ので、
自分ごと殺して途中で止まる（実際に commit を取りこぼした）。`TaskStop` を使う。

### とくに残りやすいもの

- **待機ループ**（`until grep -q ... ; do sleep ...; done`）。
  待っている相手のテストを途中で止めると、条件が永遠に来なくて回り続ける。
  相手を止めたら、待っているほうも必ず一緒に止める
- **テスト用のローカルサーバー**。`tests/run.mjs` は自分で立てて自分で片づけるので、
  手で `python3 -m http.server` を立てないこと。すでに動いていればそれを使う
- **Playwright のブラウザ**。スクリプトの最後で `await browser.close()`

## テストはリポジトリの中に置く

`tests/` の下。作業用の一時ディレクトリに置くと、セッションが切れたときに消える
（一度失っている）。スクラッチパッドは使い捨ての下書きだけに使う。
